# Using the Devcontainer

TODO: Add main instructions

## Troubleshooting

### General Tips

- Check if all required LaTeX packages are installed. Run
    ```bash
    tlmgr install `cat .devcontainer/latex-packages.txt`
    ```
  to install the latest version of the list of packages needed.
