\Chapter{Counting and Cardinality}
\Lettrine[loversize=0.1,nindent=2pt]{Ironically}, it is often the case that rigorously defining something intuitive turns out to be exceedingly tricky. Such is the case of counting. Counting the number of things in a collection seems obvious, but creating a watertight protocol for counting is more challenging than it appears at first glance.

\section{The Problem With `Intuitive' Counting}
TODO: Add
% Give an example of how systems could "count" the number of items in a set differently

\section{An Introduction to Cardinality}
TODO: Add
% Introduce Cantor and his three definitions:
%   - Define that |A| <= |B| iff injection exists
%   - Define that |A| >= |B| iff surjection exists
%   - Define that |A| = |B| iff bijection exists

\section{Finite Counting}
TODO: Add
% Explain how we can use cardinality to define the size of finite sets, by mapping a finite set's elements to one of the counter sets

\section{Infinite Sets}
TODO: Add
% Explain the definition of a finite set (\cite[p.~262]{boolos_1998})
% Define infinite sets (i.e., non-finite sets)

\section{Infinite Counting and Cantor's Theorem}
TODO: Add
% Give the example of how \N and \Successor{\N} have the same cardinality
% Explain that "sizes" don't work for infinite sets, and that cardinality is the better term for this
% Exercise: |A| < |\Power{A}| for ANY set A, including infinite sets
