\Chapter{Introduction to Sets}
\Lettrine[loversize=0.1,nindent=0pt]{The} most fundamental object that we will be using in our exploration of the structure of numbers is the set. Sets are omnipresent throughout mathematics, so it is important that one is fluent in the language of sets. This chapter serves as an introduction to sets, as we will be formally defining what they are in a later chapter.

\section{What Are Sets?}
\begin{definition}[A Simple Definition of Sets]
    A \term{set}\index{set} is a collection of things called \term{elements}\index{element} of the set.
\end{definition}

Of course, we will formally define what sets are in a future chapter, but this would suffice for now.

A set is often described by listing elements separated by commas, or by a characterizing property of its elements, within braces ($\{ \ \}$).

\begin{example}[Examples of Sets]
    \begin{itemize}[leftmargin=*]
        \item The collection $\{a, b, c\}$ is a set with 3 elements, namely $a$, $b$, and $c$. This is read ``the set containing the elements $a$, $b$, and $c$''.
        \item The collection $\{2, 3, 4, 5\}$ is a set with 4 elements, namely 2, 3, 4, and 5.
        \item The collection $\{\{1, 2, 3\}, \{4, 5\}, \{6, \{7\}\}\}$ is a set with 3 elements, which are also sets. Namely, it contains the set $\{1, 2, 3\}$, containing the elements 1, 2, and 3, the set $\{4, 5\}$, containing the elements 4 and 5, and the set $\{6, \{7\}\}$, containing the number 6 and the set $\{7\}$ which contains a single element 7.
    \end{itemize}
\end{example}

What if a set has no elements?
\begin{definition}[Empty Set]
    The \term{empty set}\index{set!empty} is the set $\{\}$ and is denoted by $\emptyset$. That is, $\emptyset = \{\}$.
\end{definition}

The empty set will come in useful when we are defining the natural numbers.

Let's now look at set equality.

\begin{definition}[Set Equality]\index{set!equality}
    Two sets $A$ and $B$ are equal if and only if they contain the same elements.
    
    To reflect that the sets $A$ and $B$ are equal to each other, we write $A = B$. If the sets $A$ and $B$ are not equal to each other, we instead write $A \neq B$.
\end{definition}

\begin{example}
    Consider the sets $\{1, 2, 3, 4\}$, $\{4, 3, 2, 1\}$, and $\{3, 4, 1, 2\}$. These sets are equal to each other even though their elements are listed in a different order. Thus we can write
    \[
        \{1, 2, 3, 4\} = \{4, 3, 2, 1\} = \{3, 4, 1, 2\}.
    \]
    However $\{1, 2, 3, 4\}$ and $\{1, 2, 3, 5\}$ are not not equal since the elements in the two sets differ. Hence,
    \[
        \{1, 2, 3, 4\} \neq \{1, 2, 3, 5\}.
    \]
\end{example}

It is important to emphasise that the order of elements in the sets do not matter.

We have specific notation to denote that an object is in a set.

\begin{definition}
    Let $A$ be a set. We write $x \in A$ if and only if $x$ is an element of $A$. Otherwise we write $x \notin A$.
\end{definition}

\begin{example}
    Consider the set $A = \{1, 2, 3\}$. Then clearly 1, 2, and 3 are elements of the set $A$, and so we may write $1 \in A$, $2 \in A$, and $3 \in A$. We could also write this more compactly as $1, 2, 3 \in A$. On the other hand, one sees that 0, -1, and 4 are not elements of the set, so $0 \notin A$, $-1 \notin A$, and $4 \notin A$ (or $0, -1, 4 \notin A$).
\end{example}

To end this section, we introduce the idea of subsets.

\begin{definition}[Subsets]
    Let $A$ and $B$ be sets.
    \begin{itemize}
        \item $A$ is a \term{subset}\index{subset} of $B$ if and only if all elements of $A$ are elements of $B$. This is denoted by $A \subseteq B$.
        \item $A$ is a \term{proper subset}\index{subset!proper} if and only if $A$ is a subset of $B$ and $B$ contains at least one element not in $A$. In this case, we write $A \subset B$.
    \end{itemize}
\end{definition}

\begin{example}
    Consider the sets $A = \{1, 2\}$, $B = \{1, 2, 3\}$, $C = \{1, 4\}$, and $S = \{1, 2, 3\}$. Then $A \subseteq S$ since the elements of $A$, namely 1 and 2, also appear in $S$. Also $B \subseteq S$ since all elements of $B$ appear in $S$. However $C \not\subseteq S$ since 4 is not an element of $S$.

    We note that $B \not\subset S$ since $S$ does not contain an element that is not in $B$. But $A \subset S$ since 3 is not in $A$.
\end{example}

\begin{exercise}
    Let $S$ be a non-empty set where $S \neq \{\emptyset\}$. Determine whether the following are true or false.
    \begin{partquestions}{\alph*}[2]
        \item $\{1, 2\} \subset \{1, 2, 3, 4\}$
        \item $\{1, 2, 3\} \subseteq \{1, 2, 4\}$
        \item $\emptyset \subseteq \emptyset$
        \item $S \subset S$
        \item $S \in \{S, \emptyset\}$
        \item $\{S\} \notin \{S, \emptyset\}$
        \item $S \subseteq \{S, \emptyset\}$
        \item $\{S\} \subseteq \{S, \emptyset\}$
    \end{partquestions}
\end{exercise}

\section{Set-Builder Notation}
For all the sets above, we were able to list down all the elements of the set. Sometimes, this is hard to do, especially when there are many elements in the set. In these cases, we use set-builder notation to describe the sets.

\begin{definition}[Set-Builder Notation]\index{set-builder notation}
    A set $S$ defined using \term{set-builder notation}\index{set!builder notation} has the syntax
    \[
        S = \{x \ | \ P(x)\},
    \]
    where the elements of $S$ are all $x$ that satisfy the predicate $P(x)$. The vertical line can be read as ``such that'' or ``satisfying''.
\end{definition}
\begin{remark}
    Some authors use the colon instead of a vertical line. For example, they may write $S = \{x \ : \ P(x)\}$.
\end{remark}

\begin{example}[Set of Even Integers]
    Consider the set of even integers,
    \[
        E = \{\dots, -6, -4, -2, 0, 2, 4, 6, \dots\},
    \]
    but defining it in this way is quite imprecise. If we let $\Z$ be the set of integers, then we can say that
    \[
        E = \{x \vert (x \in \Z) \land (\exists n \in \Z \text{ s.t. } x = 2n)\},
    \]
    i.e. the set $E$ is the set of all $x$ such that $x$ is an integer and that there is another integer $n$ such that $x = 2n$.
\end{example}

It may be a little awkward to specify that ``$x \in \Z$'' along with the actual condition ``$\exists n \in \Z \text{ s.t. } x = 2n$'' in the predicate inside the set-builder notation. In such cases, we appeal to the following definition to have cleaner notation.

\begin{definition}[Set-Builder Notation with a Domain]
    Let $D$ be a set. The set
    \[
        \{x \in D \ | \ P(x)\}
    \]
    is defined to be equal to the set
    \[
        \{x \ | \ (x \in D) \land P(x)\}.
    \]
\end{definition}

\begin{example}
    Let $\Z$ be the set of integers and $\Q$ be the set of rational numbers.
    \begin{itemize}
        \item The set $\{x \in \Z \vert (x \geq -2) \land (x \leq 3)\} = \{-2, -1, 0, 1, 2, 3\}$.
        \item The set $\{x \in \Q \vert x^2 = 2\} = \emptyset$.
        \item The set $\{x \in \Q \vert \exists n \in \Z \text{ s.t. } 3x = n\}$ is the set of all rational numbers with a denominator of 3.
    \end{itemize}
\end{example}

There is one more useful variant of set-builder notation -- one where we can have a more complex expression on the left-hand side.

\begin{definition}[Set-Builder Notation with Expressions]
    Let $f(x)$ be an expression. The set
    \[
        \{f(x) \vert P(x)\}
    \]
    is defined to be equal to the set
    \[
        \{y \vert \exists x \text{ s.t. } ((y = f(x)) \land P(x))\}
    \]
    where $P(x)$ is a predicate.
\end{definition}

\begin{example}
    Recall that we can define the set of even integers as
    \[
        E = \{x \vert (x \in \Z) \land (\exists n \in \Z \text{ s.t. } x = 2n)\}.
    \]
    Using our new definitions, we can write $E$ as
    \[
        E = \{x \in \Z \vert \exists n \in \Z \text{ s.t. } x = 2n\}
    \]
    or
    \[
        E = \{2x \vert x \in \Z\}.
    \]
\end{example}

With set-builder notation we can define an important set -- the power set of any set.

\begin{definition}[Power Set]
    Let $A$ be a set. The \term{power set}\index{power set} of $A$ is
    \[
        \Power{A} = \{S \vert S \subseteq A\},
    \]
    i.e. $\Power{A}$ is the set of all subsets of $A$.
\end{definition}

\begin{example}
    \begin{itemize}[leftmargin=*]
        \item $\Power{\emptyset} = \{\emptyset\}$, since the empty set is always a subset of any set, including itself.
        \item $\Power{\{1\}} = \{\emptyset, \{1\}\}$.
        \item $\Power{\{1, 2\}} = \{\emptyset, \{1\}, \{2\}, \{1, 2\}\}$.
        \item $\Power{\{1, 2, 3\}} = \{\emptyset, \{1\}, \{2\}, \{3\}, \{1, 2\}, \{1, 3\}, \{2, 3\}, \{1, 2, 3\}\}$.
    \end{itemize}
\end{example}

\begin{exercise}[Building Sets]
    Let $\Z$ be the set of integers and $\Q$ be the set of rational numbers.
    \begin{partquestions}{\alph*}
        \item What is the set $\left\{\frac pq \vert (p, q \in \Z) \land (q \neq 0)\right\}$?
        \item What is the set $\left\{x \in \Q \vert \frac12 x \in \Z\right\}$? Describe it in English.
        \item What is the set $\left\{x \vert (x \in \Z) \land \left(\frac{x+1}{2} \in \Z\right)\right\}$? Describe it in set-builder notation with an expression on the left-hand side.
        \item What is $\Power{\{\emptyset\}}$?
    \end{partquestions}
\end{exercise}

\section{Set Operations}
TODO: ADD

% Introduce basic operation on sets, including union, intersection, difference, cartesian product

% TODO: Where should the power set be placed?

% TODO: Where should cardinality be placed?

\section{An Aside: Quantified Statements}  % Should this be numbered?
TODO: ADD

% Introduce notation about specifying the domain of discourse
