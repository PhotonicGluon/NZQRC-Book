\section{Proofs}
\begin{questions}
    % Direct Proof
    \item Suppose $0 < x < 1$. Then $-1 < -x < 0$, meaning $0 < 1 - x < 1$. Therefore $x > 0$ and $1-x > 0$, so their product $x(1-x) > 0$.
\end{questions}
